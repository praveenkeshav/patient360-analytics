sources:
  - name: ehr_raw
    database: "{{ target.database }}"
    schema: ehr_raw

    tables:
      - name: raw_patients
      - name: raw_encounters
      - name: raw_conditions
      - name: raw_labs

  - name: claims_raw
    database: "{{ target.database }}"
    schema: claims_raw

    tables:
      - name: raw_claims
      - name: raw_claim_transactions
